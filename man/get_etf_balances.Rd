% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/institutions.R
\name{get_etf_balances}
\alias{get_etf_balances}
\title{Get US Spot ETF Balances (Latest)}
\usage{
get_etf_balances(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)
}
\arguments{
\item{asset}{This is the asset to get information on. The list of available assets is on the GN API site}

\item{since, until}{Ignored. Accepted for interface consistency; this endpoint only ever returns the latest snapshot}

\item{frequency}{A resolution for the data. See API documentation but it defaults to 24h}

\item{api_key}{The API key to use. By default it will check the GN_API_KEY environmental variable}

\item{as_date}{A logical to return a date-time object or a date object for daily observations}
}
\value{
Returns a tibble with a datetime column and one balance column per ETF ticker (prefixed \code{balance_}) and one change column per ticker (prefixed \code{changes_})
}
\description{
The most recent aggregate BTC/ETH balance held by US spot ETFs, broken
down per ETF ticker (plus a \code{total} column).
}
\details{
This is a snapshot-only endpoint: Glassnode always returns just the
latest observation for it, so \code{since}/\code{until} are accepted for
interface consistency with the rest of the package but have no effect.
}
\examples{
\dontrun{
#Need a valid API key to run
x <- get_etf_balances()
}
}
