% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treasuries.R
\name{get_treasury_balance_companies}
\alias{get_treasury_balance_companies}
\alias{get_treasury_balance_governments}
\alias{get_treasury_balance_relative_companies}
\alias{get_treasury_balance_relative_governments}
\title{Get Corporate/Government Treasury Balances}
\usage{
get_treasury_balance_companies(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)

get_treasury_balance_governments(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)

get_treasury_balance_relative_companies(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)

get_treasury_balance_relative_governments(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)
}
\arguments{
\item{asset}{This is the asset to get information on. The list of available assets is on the GN API site}

\item{since, until}{A POSIX compatible date-time object. It's converted to a unix date number}

\item{frequency}{A resolution for the data. See API documentation but it defaults to 24h}

\item{api_key}{The API key to use. By default it will check the GN_API_KEY environmental variable}

\item{as_date}{A logical to return a date-time object or a date object for daily observations}
}
\value{
Returns a tibble with a datetime column and one balance column per tracked entity (prefixed with the function's metric name), plus an \code{_aggregated} total column
}
\description{
The aggregate amount of the asset held on the balance sheets of publicly
tracked corporate treasuries or sovereign/government entities (e.g.
MicroStrategy-style corporate holders, or nation-state reserves).
}
\examples{
\dontrun{
#Need a valid API key to run
x <- get_treasury_balance_companies()
}
}
