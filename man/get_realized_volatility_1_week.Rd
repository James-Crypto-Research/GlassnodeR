% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/market.R
\name{get_realized_volatility_1_week}
\alias{get_realized_volatility_1_week}
\alias{get_realized_volatility_2_weeks}
\alias{get_realized_volatility_1_month}
\alias{get_realized_volatility_3_months}
\alias{get_realized_volatility_6_months}
\alias{get_realized_volatility_1_year}
\alias{get_realized_volatility_all}
\title{Get Realized Volatility}
\usage{
get_realized_volatility_1_week(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)

get_realized_volatility_2_weeks(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)

get_realized_volatility_1_month(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)

get_realized_volatility_3_months(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)

get_realized_volatility_6_months(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)

get_realized_volatility_1_year(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)

get_realized_volatility_all(
  asset = "BTC",
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)
}
\arguments{
\item{asset}{This is the asset to get information on. The list of available assets is on the GN API site}

\item{since, until}{A POSIX compatible date-time object. It's converted to a unix date number}

\item{frequency}{A resolution for the data. See API documentation but it defaults to 24h}

\item{api_key}{The API key to use. By default it will check the GN_API_KEY environmental variable}

\item{as_date}{A logical to return a date-time object or a date object for daily observations}
}
\value{
Returns a tibble with columns for the datetime and realized volatility. \code{get_realized_volatility_all()} instead returns one \code{realized_volatility_<window>} column per window (1w, 2w, 1m, 3m, 6m, 1y)
}
\description{
Realized (historical) price volatility computed over a rolling window. These
functions are grouped by window length.
}
\examples{
\dontrun{
#Need a valid API key to run
x <- get_realized_volatility_1_month()
}
}
