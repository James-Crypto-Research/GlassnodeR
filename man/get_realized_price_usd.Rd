% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/market.R
\name{get_realized_price_usd}
\alias{get_realized_price_usd}
\title{Get Realized Price}
\usage{
get_realized_price_usd(
  since = NULL,
  until = NULL,
  frequency = "24h",
  api_key = Sys.getenv("GN_API_KEY"),
  as_date = TRUE
)
}
\arguments{
\item{since, until}{A POSIX compatible date-time object. It's converted to a unix date number}

\item{frequency}{A resolution for the data. See API documentation but it defaults to 24h}

\item{api_key}{The API key to use. By default it will check the GN_API_KEY environmental variable}

\item{as_date}{A logical to return a date-time object or a date object for daily observations}
}
\value{
Returns a tibble with columns for the datetime and realized price
}
\description{
Realized Price represents the aggregate acquisition cost basis across the
total supply (Realized Cap divided by circulating supply). It differs from
\code{get_realized_price()}, which returns the median rather than the
aggregate realized price.
}
\examples{
\dontrun{
#Need a valid API key to run
x <- get_realized_price_usd()
}
}
